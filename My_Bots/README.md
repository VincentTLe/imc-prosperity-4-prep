# Bots Folder

- `temp_bot.py`: current working bot for rapid edits
- `my_first_bot.py`: cleaner baseline bot
- `archive/`: older snapshots moved out of the main workflow

Use `temp_bot.py` while iterating quickly, then copy or rename stable versions when you want to keep a milestone.
